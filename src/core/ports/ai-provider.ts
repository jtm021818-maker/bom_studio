import type { AiProvider } from '@/core/types/ai';

/**
 * Port for AI generation provider.
 * Implementations: OpenAI adapter (real) or Dummy adapter (fallback when no API key).
 */
export type { AiProvider };
