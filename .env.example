# ============================================
# 봄결 스튜디오 — 환경 변수
# ============================================

# --- Supabase ---
# Supabase 프로젝트 URL (대시보드 > Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# Supabase 익명 키 (클라이언트 사이드 접근용)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
# Supabase 서비스 역할 키 (서버 사이드 전용, 절대 클라이언트 노출 금지)
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# --- 데이터베이스 ---
# 런타임용 DB 접속 URL (앱 역할, RLS 적용)
DATABASE_URL=postgresql://app_role:password@db.your-project.supabase.co:5432/postgres
# DDL/마이그레이션용 DB 접속 URL (superuser, RLS 우회)
DATABASE_URL_ADMIN=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres

# --- Better Auth ---
# 인증 시크릿 키 (openssl rand -hex 32 로 생성)
BETTER_AUTH_SECRET=your-secret-key-here
# 인증 서버 URL (배포 시 실제 도메인으로 변경)
BETTER_AUTH_URL=http://localhost:3000

# --- Google OAuth ---
# Google Cloud Console > OAuth 2.0 클라이언트 ID
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# Google OAuth 클라이언트 시크릿
GOOGLE_CLIENT_SECRET=your-google-client-secret

# --- OpenAI ---
# OpenAI API 키
OPENAI_API_KEY=sk-...
# 임베딩 모델 (기본값: text-embedding-3-small)
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
# 채팅 모델 (기본값: gpt-4o-mini)
OPENAI_CHAT_MODEL=gpt-4o-mini

# --- 토스페이먼츠 ---
# 토스페이먼츠 시크릿 키 (서버 사이드)
TOSS_PAYMENTS_SECRET_KEY=test_sk_...
# 토스페이먼츠 클라이언트 키 (클라이언트 사이드)
TOSS_PAYMENTS_CLIENT_KEY=test_ck_...
# 토스페이먼츠 웹훅 시크릿
TOSS_PAYMENTS_WEBHOOK_SECRET=your-webhook-secret

# --- 모두싸인 (전자계약) ---
# 모두싸인 API 키
MODUSIGN_API_KEY=your-modusign-api-key
# 모두싸인 웹훅 시크릿
MODUSIGN_WEBHOOK_SECRET=your-modusign-webhook-secret

# --- 애플리케이션 ---
# 관리자 이메일 (쉼표 구분)
ADMIN_EMAILS=admin@bomgyeol.studio
# 앱 공개 URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
